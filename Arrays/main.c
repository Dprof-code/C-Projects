#include <stdio.h>
#include <stdlib.h>

/**
*Arrays
*/

int main()
{
    char likes[10000] = {"Praying", "Coding", "Eating", "Sleeping"};
    int evenNum[] = {2, 4, 6, 8, 10};

    printf("My favorite number is %d", evenNum[1]);

    return (0);
}
